version: '3'
services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=Cv-service'
      - 'POSTGRES_USER=dorogokupets'
      - 'POSTGRES_PASSWORD=ltl123'
    ports:
      - '5432:5432'
